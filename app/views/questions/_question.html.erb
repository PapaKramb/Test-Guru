<p>
  <td colspan = 3></th>
  <td><%= question.body %></td>
  <td><%= link_to 'Show', question_path(question) %></td>
  <td><%= link_to 'Edit', edit_question_path(question) %></td>
  <td><%= link_to 'Delete', question_path(question), action: :destroy, method: :delete, data: { confirm: 'You are sure?' } %></td>
</p>
