<%= form_with model: [:admin, @question, @answer], local: true do |f| %>
  <%= render 'shared/errors', resource: @answer %>

  <p>
    <%= f.label :answer %>
    <%= f.text_field :body %>
  </p>

  <p>
    <%= f.label :correct %>
    <%= f.check_box :correct %>
  </p>

  <p>
   <%= f.submit %>
  </p>

<% end %>